const should = require('should');
const Link = require('./../lib/link').Link;

describe('Testing Link function', function () {
  it('should  creating the Link object correctly', function () {
    const link = new Link();

  });
});